# 实验三：中文科技事件信息抽取实验系统

## 一、实验目的

信息检索解决的是“从文档集合中找到相关文档”的问题，信息抽取则更进一步：它希望从非结构化文本里取出可以直接使用的结构化信息。本实验选择中文科技新闻作为语料，主要考虑两个原因。第一，科技新闻中经常出现产品发布、系统升级、模型开源、芯片性能提升等事件，标题和正文中包含较明显的事件要素。第二，这一领域和实验二的信息检索系统数据来源一致，便于在已有语料采集、存储和界面基础上继续扩展。

本实验围绕“科技事件信息抽取”展开。系统从新闻中抽取研发主体、技术产品、事件动作、版本或指标、事件日期等字段，并把这些字段组合成一个事件。例如：

```text
华为 在 2026-06-01 发布 HarmonyOS NEXT 5.0，相关指标为 5.0。
```

这种结构化结果比单纯的关键词更容易用于事件筛选、人工评价和后续知识组织。

## 二、实验环境

实验环境为 Windows + Python 虚拟环境。开发和测试时使用项目目录下的 `.venv`，核心版本如下：

| 环境或依赖 | 版本 |
| --- | --- |
| Python | 3.13.2 |
| streamlit | 1.58.0 |
| pandas | 3.0.3 |
| jieba | 0.42.1 |
| aiohttp | 3.14.0 |
| beautifulsoup4 | 4.14.3 |
| easyocr | 1.7.2 |
| openai | 2.38.0 |
| pillow | 12.2.0 |
| torch | 2.12.0+cu126 |
| transformers | 5.10.1 |
| qwen-vl-utils | 0.0.14 |

本地机器配有 NVIDIA GeForce RTX 4060 Laptop GPU，显存 8GB。普通文本抽取和 OCR 测试不强依赖 GPU；本地多模态大模型推理会占用较多显存，因此在系统中作为可选路径保留。

创建和启动环境的命令如下：

```powershell
python -m venv .venv
.venv\Scripts\Activate
pip install -r requirements.txt
streamlit run app.py
```

如果需要测试本地视觉语言模型，可另外安装 CUDA 版 PyTorch：

```powershell
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
```

## 三、语料来源与数据组织

本实验使用中文科技新闻数据。爬虫配置中包含 IT之家、36氪科技、新浪科技、凤凰网科技、新华网科技、人民网科技、网易科技、环球网科技、澎湃新闻、和讯科技等来源。当前清洗后的语料在 `source` 字段中统一标记为“科技新闻”，但每篇文档保留原始 URL，可以从 URL 追溯具体网站。

本地语料主要保存在：

```text
data/documents.json
data/raw_news/tech_news_20260604_144724.json
```

当前语料规模为 800 篇，超过课程实验所需的 100 篇。单篇文档主要包含以下字段：

| 字段 | 含义 |
| --- | --- |
| `id` | 文档编号 |
| `title` | 新闻标题 |
| `summary` | 摘要或正文前若干字符 |
| `content` | 新闻正文 |
| `url` | 原文链接 |
| `source` | 新闻来源标记 |
| `publish_time` | 发布时间 |

数据采集后会进行基本清洗，包括空文本过滤、标题长度检查、URL 去重、正文空白符规范化等。系统启动时优先读取作业三目录内的本地语料；开发阶段参考过实验二目录，但目前作业三项目已经能够独立运行。

## 四、系统总体设计

系统按“语料读取、字段抽取、结果展示、人工评价”几部分组织。整体流程如下：

```text
本地新闻语料
    -> 文本清洗与字段规范化
    -> 候选标题 / 摘要 / 正文组合
    -> 正则、开源 NLP 或 API 抽取
    -> 事件字段组装
    -> JSON / CSV 结果保存
    -> 图形化展示与筛选
    -> 人工修正与指标计算
    -> OCR / 多媒体抽取扩展
```

项目中的主要模块如下：

| 文件或目录 | 作用 |
| --- | --- |
| `crawler/` | 异步爬虫、网页解析和数据清洗 |
| `extractor/regex_extractor.py` | 基础正则和优化正则抽取 |
| `extractor/opensource_nlp_extractor.py` | 基于 `jieba` 的开源 NLP 抽取 |
| `extractor/nlp_extractor.py` | DeepSeek API 抽取 |
| `multimodal/` | OCR、本地多模态模型、多模态 API 相关代码 |
| `utils/pipeline.py` | 语料加载、批量抽取、统计和保存 |
| `evaluator/metrics.py` | 字段级评价指标计算 |
| `scripts/run_extraction.py` | 命令行抽取入口 |
| `scripts/evaluate_extraction.py` | 命令行人工评价指标计算 |
| `app.py` | Streamlit 图形化界面 |

图形化界面包含系统概览、事件抽取、事件详情、人工评价、评价指标和多媒体抽取几个页面。命令行脚本则更适合做批量实验和结果复现。

## 五、事件定义与信息点设计

科技新闻里常见的事件可以概括为“某个主体在某个时间围绕某项技术产品做了某个动作，并伴随一个版本或指标变化”。本实验据此设计了 5 个字段：

| 字段 | 含义 | 示例 |
| --- | --- | --- |
| `developer` | 研发主体、发布主体、企业或机构 | 华为、三星、Marvell、微软 |
| `tech_product` | 产品、芯片、模型、系统、平台或开源项目 | HarmonyOS NEXT 5.0、Teralynx T100、macOS 26.5.1 |
| `action_type` | 事件动作 | 发布、展示、升级、开源、适配、修复 |
| `version_metric` | 版本号、性能指标、参数、价格、容量等 | 240Hz、102.4 Tbps、128GB、20% |
| `date` | 新闻或事件日期 | 2026-06-04 |

事件可以写成如下模板：

```text
developer 在 date 对 tech_product 执行 action_type，相关版本或指标为 version_metric。
```

这个模板不追求覆盖所有新闻事实。像融资、裁员、政策监管、娱乐新闻等内容，即使出现在科技网站中，也不一定是本实验关注的“科技产品事件”。这也是后面人工评价中会出现误抽样本的原因。

## 六、信息抽取算法设计

### 1. 正则表达式抽取

正则抽取是本系统最基础的方法。基础版 `BasicRegexExtractor` 只使用较少的标题规则，主要用于对比；优化版 `OptimizedRegexExtractor` 是系统默认的规则抽取器。

基础正则主要匹配以下模式：

| 字段 | 规则思路 |
| --- | --- |
| `developer` | 常见科技公司、机构名称匹配 |
| `tech_product` | 英文型号、书名号、芯片/模型/系统等后缀 |
| `action_type` | 发布、推出、升级、开源、适配等触发词 |
| `version_metric` | 版本号、百分比、容量、频率、吞吐量、价格等 |
| `date` | 发布时间或正文中的日期表达 |

优化正则在此基础上加入了领域词典、触发词归一化和更多指标规则。比如“亮相、推出、宣布”会被统一归到较稳定的动作类别中；`2026年6月4日`、`2026-6-4` 这类日期会归一化成 `YYYY-MM-DD`。这种方法的好处是速度快、可解释、离线可复现。它的问题也很明显：标题一旦写得口语化，或者产品名边界不清，正则容易抽偏。

### 2. 开源 NLP / jieba 辅助抽取

系统实现了 `OpenSourceNLPExtractor`，调用 `jieba` 进行中文分词和词性标注，再结合领域词典和规则完成字段抽取。其流程可以概括为：

```text
标题 / 摘要 / 正文
    -> jieba 分词
    -> 词性标注
    -> 主体候选识别
    -> 产品候选识别
    -> 动作词归一化
    -> 正则补充日期和指标
    -> 输出事件字段
```

开源 NLP 方法的覆盖面比纯正则更广，尤其在 `developer` 和 `tech_product` 两个字段上更容易给出候选。但它并不等同于真正的语义理解。新闻标题中的品牌、人名、媒体名、营销短语经常混在一起，`jieba` 的词性结果只能提供辅助判断，不能完全解决边界问题。

### 3. DeepSeek API 抽取

系统还接入了 DeepSeek API，用作语义抽取的对比方法。API Key 存放在本地 `.env` 文件中，不进入 Git 提交。配置项大致如下：

```text
LLM_API_URL=https://api.deepseek.com
LLM_API_KEY=本地私有 API Key
LLM_MODEL=deepseek-v4-flash
LLM_TEMPERATURE=0.1
LLM_MAX_TOKENS=800
```

提示词要求模型严格返回 JSON，并且只包含 `developer`、`tech_product`、`action_type`、`version_metric`、`date` 五个字段。API 抽取在语义理解上更灵活，例如能把“Marvell 发布 102.4 Tbps 交换芯片 Teralynx T100，宣称节能 25%”解析成更自然的结构。但它需要网络和账户余额，结果也会受模型版本影响。系统因此没有把 API 作为唯一方案，而是允许用户选择抽取篇数，通常先用 2 到 5 篇测试。

### 4. 多媒体抽取尝试

多媒体部分主要处理科技海报、发布会截图或产品图。系统提供三条路径：

| 路径 | 当前情况 |
| --- | --- |
| OCR + 正则 | 已跑通，默认使用 EasyOCR |
| 本地多模态模型 | 接口已实现，默认模型为 Qwen/Qwen2.5-VL-3B-Instruct |
| 多模态 API | 前端预留配置，可填写云端多模态模型接口 |

实际测试中，`data/images/test1.png` 通过 EasyOCR 识别出如下文字片段：

```text
华为正式发布 HarmonyOs NEXT 5.0
发布日期: 2026-06-01
核心能力: 端侧 AI 性能提升 35%
支持设备: 手机、平板、车机
```

随后系统抽取出：

| 字段 | 结果 |
| --- | --- |
| `developer` | 华为 |
| `tech_product` | HarmonyOs NEXT 5.0 |
| `action_type` | 发布 |
| `version_metric` | 5.0 |
| `date` | 2026-06-01 |

OCR 结果中其实已经出现“性能提升 35%”，但优化正则最终取到了版本号 `5.0`。这说明多媒体链路是通的，同时也暴露出指标字段仍有优化空间。

## 七、人工评价与指标计算

人工评价按字段进行。对每个被评价样本，人工阅读新闻标题和系统抽取结果，给出该字段的标准答案。评价时使用 TP、FP、FN 三类计数：

| 记号 | 含义 |
| --- | --- |
| TP | 系统抽取了字段，且人工判断正确 |
| FP | 系统抽取了字段，但人工判断错误 |
| FN | 人工认为应抽取，但系统漏抽或抽错 |

指标计算公式为：

$Precision=\frac{TP}{TP+FP}$

$Recall=\frac{TP}{TP+FN}$

$F1=\frac{2 \times Precision \times Recall}{Precision+Recall}$

系统按字段分别计算指标。Macro Precision、Macro Recall、Macro F1 是对五个字段的对应指标取平均，用于观察整体表现。

本次实验对优化正则结果人工标注了 34 条样本。标注文件和指标文件分别为：

```text
data/evaluations/annotations_regex.json
data/evaluations/metrics_regex.json
```

命令行计算方式：

```powershell
python scripts\evaluate_extraction.py --target regex
```

输出摘要如下：

```text
评价对象: 优化正则结果
标注样本数: 34
抽取结果数: 800
Macro F1: 0.6976
```

字段级评价结果如下：

| 字段 | TP | FP | FN | Precision | Recall | F1 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| developer | 18 | 7 | 9 | 0.7200 | 0.6667 | 0.6923 |
| tech_product | 19 | 15 | 0 | 0.5588 | 1.0000 | 0.7170 |
| action_type | 13 | 18 | 3 | 0.4194 | 0.8125 | 0.5532 |
| version_metric | 10 | 13 | 4 | 0.4348 | 0.7143 | 0.5405 |
| date | 33 | 1 | 0 | 0.9706 | 1.0000 | 0.9851 |
| Macro Avg | - | - | - | 0.6207 | 0.8387 | 0.6976 |

这个结果和人工观察基本一致。日期字段最稳定，产品字段召回高但边界容易偏长；动作类型和版本指标更难，尤其是标题中同时出现价格、容量、版本号、刷新率、销量时，规则很难判断哪一个才是最核心的指标。

## 八、系统运行与功能展示

命令行和图形界面都可以完成实验流程。较完整的测试步骤如下。

### 1. 创建虚拟环境并安装依赖

```powershell
python -m venv .venv
.venv\Scripts\Activate
pip install -r requirements.txt
```

### 2. 检查本地语料

当前语料已经保存在 `data/documents.json` 和 `data/raw_news/` 下。测试结果显示：

```text
crawler_import=OK
current_document_count= 800
needs_crawling= False
corpus_total_articles= 800
```

### 3. 命令行抽取

```powershell
python scripts\run_extraction.py --help
python scripts\run_extraction.py --extractor basic --limit 5
python scripts\run_extraction.py --extractor optimized --limit 5
python scripts\run_extraction.py --extractor open_nlp --limit 5
python scripts\run_extraction.py --extractor nlp --limit 2
python scripts\run_extraction.py --extractor comparison
```

实际运行结果摘要如下：

| 测试项 | 结果 |
| --- | --- |
| 语法检查 | 25 个 Python 文件，`syntax_errors=NO` |
| 基础正则小样本 | 5 条样本，较完整事件 2 条 |
| 优化正则小样本 | 5 条样本，较完整事件 5 条 |
| 开源 NLP 小样本 | 5 条样本，较完整事件 5 条 |
| DeepSeek API 小样本 | 2 条样本，较完整事件 2 条 |
| 全量优化正则 | 800 条结果，较完整事件 547 条 |
| 全量开源 NLP | 800 条结果，较完整事件 614 条 |
| OCR 图片抽取 | EasyOCR 跑通，生成 5 字段事件 |

### 4. 打开图形界面

```powershell
streamlit run app.py
```

界面包含系统概览、事件抽取、事件详情、人工评价、评价指标、多媒体抽取等页面。截图位置预留如下：

```text
图1 系统概览界面：待补充截图
图2 事件抽取结果界面：待补充截图
图3 事件详情界面：待补充截图
图4 人工评价界面：待补充截图
图5 指标统计界面：待补充截图
图6 多媒体抽取界面：待补充截图
```

## 九、实验结果分析

从字段覆盖情况看，优化正则相对于基础正则提升明显。基础正则在 800 篇文档中只有 73 条较完整事件，而优化正则达到 547 条；开源 NLP 方法进一步提高到 614 条。覆盖率并不等同于准确率，但它能说明算法是否愿意给出候选结果。

全量抽取统计如下：

| 算法 | 文档数 | developer | tech_product | action_type | version_metric | date | 至少4字段完整事件 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 基础正则 | 800 | 156 | 449 | 165 | 104 | 799 | 73 |
| 优化正则 | 800 | 603 | 784 | 566 | 429 | 799 | 547 |
| 开源 NLP | 800 | 722 | 797 | 566 | 429 | 799 | 614 |
| DeepSeek API | 2 | 2 | 2 | 2 | 1 | 2 | 2 |

`date` 字段最好抽。大部分新闻都有发布时间，即使正文中没有明确事件日期，也可以用新闻日期作为近似值。`tech_product` 的覆盖率也高，但准确率不一定同样高，因为标题中可能同时出现产品名、技术名、参数和营销短语。人工评价中 `tech_product` 的 Recall 为 1.0000，但 Precision 只有 0.5588，正说明系统倾向于“多抽”，边界控制还不够细。

`developer` 比预想中更麻烦。企业名称有时出现在标题开头，有时隐藏在正文或 URL 来源中；有些标题还会出现人物、媒体名或合作方，规则容易把这些内容当成主体。`action_type` 依赖触发词，规则可以捕捉“发布、升级、开源”，但对“众筹、首销、供货不足、合作、暂缓投资”这类动作理解不够细。`version_metric` 的问题最集中：价格、容量、年份、版本号、性能提升和销量都可能是数字，正则很难仅靠局部模式判断哪个数字最重要。

DeepSeek API 在小样本中表现较自然，尤其能保留产品名和指标的语义关系。但 API 方法不适合无控制地全量调用。一方面成本较高，另一方面网络状态和模型版本会影响复现。笔者认为，它更适合作为高质量样本抽取或规则结果校正工具，而不是替代全部规则方法。

## 十、创新点与不足

本实验的改进不在于引入某一个复杂模型，而是把信息检索、事件抽取、评价和多媒体尝试放到了同一个实验系统中。相较于只输出关键词，系统现在能生成事件级结构化结果；相较于单一正则，系统提供了基础正则、优化正则、开源 NLP 和 API 抽取几种路径；人工评价模块也让系统能暴露自己的错误，而不是只展示看起来正确的样例。

具体来看，本实验做了以下几项扩展：

| 方向 | 实现 |
| --- | --- |
| 事件级抽取 | 5 个字段组合为科技事件 |
| 规则优化 | 正则结合领域词典、触发词和指标模式 |
| 开源 NLP | 使用 `jieba` 分词和词性标注辅助抽取 |
| 大模型抽取 | 接入 DeepSeek API，支持限制抽取篇数 |
| 人工评价 | 支持字段级标注、修正和指标计算 |
| 多媒体尝试 | OCR 识别图片文本，再复用事件抽取器 |
| 图形化操作 | Streamlit 页面展示抽取、详情和评价 |

不足也比较清楚。科技新闻标题噪声较大，很多文章并不是严格的产品事件；正则方法泛化能力有限，遇到新写法要继续补规则；开源 NLP 方法能扩大候选范围，但也会引入更多误识别；API 抽取效果更灵活，却要付出网络、费用和可复现性成本。复杂事件关系目前也没有建模，例如一个标题中同时出现多个企业和多个产品时，系统还不能可靠地区分主事件和附带信息。

## 十一、环境与社会可持续发展影响

系统尽量减少重复爬取。语料采集后保存到本地，后续抽取、评价和展示都读取本地 JSON 文件；如果文档数已经满足实验需要，爬虫模块会判断 `needs_crawling=False`，避免重复访问网站。

爬虫配置中设置了并发数、超时时间和请求间隔，目的是降低对目标站点的压力。语料只来自公开科技新闻，不采集个人隐私数据，也不用于自动化决策。抽取结果在展示前还提供人工评价机制，能减少错误字段被直接当作事实使用的风险。

计算资源方面，系统默认保留轻量的正则和开源 NLP 方法。DeepSeek API、本地多模态模型和 OCR 都是可选模块。这样做比较务实：大模型和多媒体模型确实能处理更复杂的输入，但没有必要在每一次实验运行中都调用它们。对于课程实验而言，先用轻量方法完成可复现实验，再在少量样本上测试复杂模型，是更合适的选择。

## 十二、总结

本实验完成了一个中文科技事件信息抽取系统。系统使用 800 篇本地科技新闻语料，抽取 `developer`、`tech_product`、`action_type`、`version_metric`、`date` 五个字段，并将它们组织为事件。系统实现了基础正则、优化正则、开源 NLP、DeepSeek API 和 OCR 多媒体抽取等方法，也提供了命令行和 Streamlit 图形化界面。

实验结果显示，规则方法在日期、显式产品名和常见动作词上比较稳定；开源 NLP 可以提高覆盖面，但会带来候选边界问题；大模型方法理解能力更强，不过成本和复现性需要控制。人工评价部分给出了更直接的反馈：优化正则的 Macro F1 为 0.6976，日期字段效果最好，动作和指标字段仍有明显提升空间。

这次实验让信息抽取任务从“能抽出一些字段”推进到“能评价这些字段是否可靠”。这一步很重要，因为结构化信息一旦进入表格或知识库，就容易被读者当成确定事实。保留人工评价和错误分析，比单纯堆叠抽取方法更有实际价值。

## 十三、成员分工

| 成员 | 学号 | 主要工作 | 具体贡献 |
| --- | --- | --- | --- |
| 待补充 | 待补充 | 数据采集与预处理 | 爬虫配置、语料清洗、本地 JSON 存储与测试 |
| 待补充 | 待补充 | 抽取算法与评价 | 正则抽取、开源 NLP、DeepSeek API、人工评价指标 |
| 待补充 | 待补充 | 图形界面与多媒体扩展 | Streamlit 页面、多媒体 OCR、测试截图与报告整理 |

## 十四、参考文献

1. Daniel Jurafsky, James H. Martin. *Speech and Language Processing*.
2. Christopher D. Manning, Prabhakar Raghavan, Hinrich Schütze. *Introduction to Information Retrieval*.
3. Python Software Foundation. Python `re` Regular Expression Documentation.
4. `jieba` 中文分词项目文档。
5. Streamlit 官方文档。
6. EasyOCR 项目文档。
7. DeepSeek API 文档。
8. Qwen2.5-VL 模型与 `qwen-vl-utils` 文档。
