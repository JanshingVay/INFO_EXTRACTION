# 信息抽取系统实验报告

## 1. 实验背景与任务理解

本实验要求设计并实现一个信息抽取实验系统。与作业2的信息检索系统不同，信息抽取的目标不是根据查询返回相关文档，而是从文档内容中识别并抽取结构化信息点。本项目选择中文科技新闻作为语料，面向“核心技术产品发布与升级事件”进行抽取。

系统抽取的事件可以表示为：

> 研发主体在某日期对某技术产品执行某类动作，并伴随版本或指标变化。

该事件由 5 个信息点组成：`developer`、`tech_product`、`action_type`、`version_metric`、`date`，满足作业3“不少于5个特定信息点，并能组合成一个事件”的要求。

## 2. 与作业2的关系

作业2已经实现中文信息检索系统，包含网络爬虫、本地文档存储、倒排索引、向量空间模型、BM25检索、人工评价和图形化界面。本作业3在作业2的语料基础上继续实现信息抽取。

具体做法是将作业2的 `INFO_RETRIEVE/data/documents.json` 转换为作业3独立语料文件：

```text
data/raw_news/from_info_retrieve.json
```

转换后作业3不再依赖 `INFO_RETRIEVE/` 目录。该设计既复用了作业2已完成的语料采集成果，也避免了重复爬取网络数据，提升了实验的稳定性和可复现性。

## 3. 数据来源与本地存储

本实验使用作业2已采集的中文科技新闻文档，共 739 篇，超过作业要求的 100 篇。语料来源包括 IT之家、新浪科技、网易科技、凤凰网科技、36氪科技、新华网科技、人民网科技等。

转换后的单篇文档包含如下字段：

```json
{
  "id": "ir_0",
  "title": "新闻标题",
  "summary": "摘要",
  "content": "正文",
  "url": "原文URL",
  "source": "新闻来源",
  "publish_time": "发布时间"
}
```

本地存储文件：

```text
data/raw_news/from_info_retrieve.json
```

## 4. 环境与社会可持续发展考虑

本系统在设计中考虑了环境和社会可持续发展影响：

1. 复用作业2已爬取并清洗的本地语料，避免重复访问目标网站，减少网络请求与服务器压力。
2. 抽取结果和评价结果全部本地缓存，避免重复计算。
3. 语料来自公开科技新闻，不采集个人隐私数据。
4. 系统用于课程实验和知识组织，不用于自动化决策或敏感场景。
5. 采用模块化结构，便于后续复用、维护和扩展，降低重复开发成本。

## 5. 系统总体架构

系统主要包含以下模块：

```text
作业2本地语料
    -> 语料转换模块
    -> 正则信息抽取模块
    -> 抽取结果JSON/CSV存储
    -> 图形化展示与筛选
    -> 人工标注与评价
    -> OCR多媒体抽取扩展
```

核心文件：

| 文件 | 功能 |
| --- | --- |
| `utils/pipeline.py` | 语料转换、批量抽取、结果统计 |
| `extractor/regex_extractor.py` | 基础正则与优化正则抽取器 |
| `evaluator/metrics.py` | 字段级 Precision / Recall / F1 |
| `app.py` | Streamlit 图形界面 |
| `desktop_app.py` | Tkinter 标准库兜底界面 |
| `multimodal/multimodal_extraction.py` | OCR图片信息抽取 |

## 6. 事件模型与信息点定义

本实验抽取 5 个事件字段：

| 字段 | 含义 | 示例 |
| --- | --- | --- |
| `developer` | 研发主体或发布主体 | 华为、OpenAI、腾讯、英伟达 |
| `tech_product` | 技术产品、模型、平台、芯片或软件 | GPT、HarmonyOS、AI芯片 |
| `action_type` | 事件动作 | 发布、推出、升级、开源、上线、修复 |
| `version_metric` | 版本号、参数量、性能指标或规模数据 | v1.2、70B参数、提升40% |
| `date` | 事件日期 | 2026-05-31 |

组合后的事件示例：

```text
华为在 2026-05-31 对 华为应用市场 进行了 发布，相关指标为 4928万次。
```

## 7. 正则抽取算法设计

作业要求至少实现正则表达式匹配算法。本系统实现了两类正则抽取器。

### 7.1 基础正则抽取器

`BasicRegexExtractor` 作为 baseline，仅使用标题和简单正则模式抽取字段，规则较少，主要用于算法对比。

### 7.2 优化正则抽取器

`OptimizedRegexExtractor` 在基础版上进行增强：

1. 领域词典：维护科技公司、研究机构、开源基金会和常见厂商名称。
2. 动作归一化：将“发布、推出、亮相、宣布”等归一到“发布”，将“更新、迭代、改版”等归一到“升级”。
3. 产品识别：结合书名号、英文产品名、中文技术产品后缀进行抽取。
4. 日期归一化：支持 `2026-5-31`、`2026/05/31`、`2026年5月31日` 等格式，统一为 `YYYY-MM-DD`。
5. 指标识别：支持版本号、参数量、百分比、GB/TB、TOPS、万次/亿次等技术指标。
6. 标题优先、正文补充：优先使用新闻标题和摘要，提高抽取效率和准确性。

## 8. 抽取结果与算法对比

系统已经对 739 篇本地文档完成批量抽取，输出文件如下：

```text
data/extraction_results/basic_regex_results.json
data/extraction_results/regex_results.json
data/extraction_results/regex_results.csv
```

批量抽取统计如下：

| 算法 | developer | tech_product | action_type | version_metric | date | 较完整事件 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 基础正则 | 146 | 437 | 139 | 100 | 738 | 57 |
| 优化正则 | 569 | 717 | 516 | 400 | 738 | 506 |

其中“较完整事件”定义为至少抽取出 4 个字段的事件。可以看到，优化正则在研发主体、技术产品、事件动作和版本指标等字段上均显著提高了覆盖能力。

## 9. 图形化界面设计

本系统提供两种图形化界面。

### 9.1 Streamlit 主界面

运行方式：

```bash
streamlit run app.py
```

页面包括：

1. 系统概览：展示语料数量、抽取结果数量、字段覆盖情况、来源分布。
2. 事件抽取：运行抽取、表格展示、筛选和下载CSV。
3. 事件详情：展示原文与结构化事件。
4. 人工评价：人工修正字段并保存标注。
5. 评价指标：计算字段级 Precision、Recall、F1。
6. 多媒体抽取：上传图片，OCR识别后抽取事件字段。

### 9.2 Tkinter 兜底界面

运行方式：

```bash
python desktop_app.py
```

该界面只依赖 Python 标准库，可在未安装 Streamlit 的环境中展示抽取结果、执行筛选、运行语料构建和抽取。

## 10. 人工评价方法

系统支持对抽取结果进行人工评价。评价流程如下：

1. 在图形界面中选择待评价事件。
2. 查看系统自动抽取的 5 个字段。
3. 人工修正字段，空字段可以留空。
4. 保存标注到：

```text
data/evaluations/annotations_from_info_retrieve.json
```

5. 系统根据人工标注计算字段级指标：

```text
Precision = TP / (TP + FP)
Recall = TP / (TP + FN)
F1 = 2 * Precision * Recall / (Precision + Recall)
```

指标输出到：

```text
data/evaluations/metrics_from_info_retrieve.json
```

该模块满足“支持抽取结果准确率人工评价”的要求。提交前可人工标注 30 至 50 篇样本，用于形成最终评价表。

## 11. 多媒体信息抽取尝试

本系统尝试了多媒体信息抽取。流程为：

```text
科技发布海报或截图 -> OCR文字识别 -> 事件抽取器 -> 结构化事件字段
```

系统支持 EasyOCR 和 PyTesseract 两种 OCR 路径。图形界面中可以上传图片，系统识别文字后复用文本事件抽取模块，输出同样的 5 个事件字段。

该扩展对应作业3扩展要求中的“多媒体信息抽取”。

## 12. 创新点与优化

本项目的创新与优化主要体现在：

1. 作业2与作业3串联：在信息检索语料基础上继续实现信息抽取，形成“文档获取-检索-抽取”的完整流程。
2. 双层正则算法：实现基础正则和优化正则，能够进行算法对比。
3. 领域词典与上下文规则融合：提高中文科技新闻事件要素的抽取覆盖能力。
4. 图形化人工评价：支持字段级人工修正和指标计算。
5. 多媒体抽取尝试：将 OCR 与事件抽取结合，用于处理科技海报和截图。
6. 独立可运行：转换后的作业3语料保存在本项目内，删除作业2参考目录后仍可运行。

## 13. 不足与改进方向

当前系统主要基于规则抽取，仍存在一些不足：

1. 某些新闻不是严格的技术发布事件，可能导致动作或产品字段误抽。
2. 中文产品名边界较复杂，仅靠正则难以完全准确。
3. 版本指标字段可能需要更细粒度的语义过滤。
4. OCR结果受图片清晰度影响较大。

后续可以引入命名实体识别、依存句法分析、少样本序列标注模型或大语言模型，对正则结果进行校正和补充。

## 14. 总结

本项目完成了一个面向中文科技新闻的事件信息抽取系统。系统基于作业2的 739 篇本地语料，抽取 5 个可组合成事件的信息点，实现了正则表达式抽取、优化算法对比、图形化展示、人工评价和 OCR 多媒体抽取尝试。整体上覆盖了作业3从基本要求到扩展要求的主要评分点。
