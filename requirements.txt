aiohttp>=3.9.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
aiofiles>=23.0
jieba>=0.42.1
Pillow>=10.0.0
pandas>=2.0.0
streamlit>=1.35.0

# 可选 OCR 引擎（安装至少一个即可）
easyocr>=1.7.0          # 推荐，中英文均可，pip install easyocr
# paddleocr>=2.7.0      # 中文最强，但依赖较大: pip install paddleocr paddlepaddle
# pytesseract>=0.3.10   # 轻量备选，需额外安装 tesseract 系统包

# 可选 LLM 抽取支持（不配置 API Key 会自动回退到 Regex）
openai>=1.0.0
